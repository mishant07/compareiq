import { type Server } from 'node:http';
import type { BrowserRuntimeProvider } from '../browser/runtime/provider.js';
export interface DaemonServerOptions {
    port?: number;
    host?: string;
    version: string;
}
export interface DaemonServerHandle {
    server: Server;
    listen(): Promise<void>;
    close(): Promise<void>;
}
/** @internal Exported for deterministic concurrency contract tests. */
export declare function ensureProfileCoalesced(provider: BrowserRuntimeProvider, alias: string, idempotencyKey: string): Promise<{
    profile: {
        id: string;
        displayName: string;
    };
    created: boolean;
}>;
export declare function createDaemonServer(provider: BrowserRuntimeProvider, opts: DaemonServerOptions): DaemonServerHandle;
