import type { Browser as PlaywrightBrowser, BrowserContext as PlaywrightBrowserContext, Page as PlaywrightPage } from 'playwright-core';
import { type BrowserRunArtifactSink, type BrowserRunOptions, type BrowserRunResult } from './types.js';
export interface BrowserRunSessionScope {
    browser: PlaywrightBrowser;
    context: PlaywrightBrowserContext;
    page: PlaywrightPage;
    pages(): readonly PlaywrightPage[];
    createPage(): Promise<PlaywrightPage>;
    onPage(listener: (page: PlaywrightPage) => void): () => void;
}
export interface BrowserRunProgramHost extends BrowserRunSessionScope {
    pageId: string;
    artifactSink?: BrowserRunArtifactSink;
}
export declare const DOWNLOAD_WAIT_TIMEOUT_HINT = "A timed-out download wait is not a license to invent file contents. If a download event fired, use download.saveAs(suggestedFilename()). If none fired, report that gap. Do not rebuild the file from page logic.";
export declare const POPUP_WAIT_TIMEOUT_HINT: string;
export declare function runBrowserProgram(input: BrowserRunProgramHost, source: string, options?: BrowserRunOptions): Promise<BrowserRunResult>;
