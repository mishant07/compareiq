export interface ChromeProcessIdentity {
    executablePath: string;
    userDataDir: string;
    port?: number;
    profileDirectory?: string;
}
export declare function matchChromeProcessCommand(command: string, identity: ChromeProcessIdentity): boolean;
export declare function findExactChromeProcesses(identity: ChromeProcessIdentity, platform?: NodeJS.Platform): Promise<number[]>;
/** Matches any Chrome process carrying --no-startup-window — real users never pass that flag; only webcmd's registration trigger does. */
export declare function findNoStartupWindowChromeProcesses(platform?: NodeJS.Platform): Promise<number[]>;
export declare function listenerBelongsToProcess(port: number, pid: number, platform?: NodeJS.Platform): Promise<boolean>;
export declare function terminateChromeProcessTree(pid: number, platform?: NodeJS.Platform, force?: boolean): Promise<void>;
