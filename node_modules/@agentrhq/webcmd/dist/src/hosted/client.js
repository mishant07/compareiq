import { ArgumentError, attachTraceReceipt, CliError, EXIT_CODES } from '../errors.js';
import { parseExecutionArtifactDownloadUrl } from './artifact-url.js';
import { HOSTED_COMMAND_ORIGIN_CAPABILITY, HOSTED_CORE_COMMANDS_CAPABILITY, isHostedCoreCommandId, } from './core-commands.js';
import { log } from '../logger.js';
import { HOSTED_SESSION_PROTOCOL_VERSION } from './types.js';
/**
 * Resolves the active workspace from CLI flags/env, precedence: --workspace flag > WEBCMD_WORKSPACE env > undefined.
 */
export function resolveWorkspace(argv, env) {
    const idx = argv.indexOf('--workspace');
    if (idx >= 0 && argv[idx + 1])
        return argv[idx + 1];
    const equalsForm = argv.find(arg => arg.startsWith('--workspace='));
    if (equalsForm !== undefined) {
        const value = equalsForm.slice('--workspace='.length).trim();
        if (value)
            return value;
    }
    const fromEnv = env.WEBCMD_WORKSPACE?.trim();
    return fromEnv ? fromEnv : undefined;
}
export class HostedClientError extends CliError {
    execution;
    trace;
    details;
    constructor(code, message, help, exitCode = EXIT_CODES.GENERIC_ERROR, metadata = {}) {
        super(code, message, help, exitCode);
        this.execution = metadata.execution;
        this.trace = metadata.trace;
        this.details = metadata.details;
        if (metadata.trace)
            attachTraceReceipt(this, metadata.trace);
    }
}
export class HostedClient {
    apiBaseUrl;
    apiKey;
    workspace;
    fetchImpl;
    signal;
    constructor(options) {
        this.apiBaseUrl = options.apiBaseUrl.replace(/\/+$/, '');
        this.apiKey = options.apiKey;
        this.workspace = options.workspace;
        this.fetchImpl = options.fetchImpl ?? fetch;
        this.signal = options.signal;
    }
    async getMe() {
        return this.request('/v1/me');
    }
    async getManifest() {
        const body = await this.request('/v1/manifest');
        const manifestMetadata = isRecord(body)
            && isRecord(body.manifest)
            && isRecord(body.manifest.metadata)
            ? body.manifest.metadata
            : undefined;
        const sessionProtocolVersion = manifestMetadata?.sessionProtocolVersion;
        if (manifestMetadata
            && (sessionProtocolVersion === undefined
                || (typeof sessionProtocolVersion === 'number'
                    && Number.isInteger(sessionProtocolVersion)
                    && sessionProtocolVersion > 0
                    && sessionProtocolVersion !== HOSTED_SESSION_PROTOCOL_VERSION))) {
            throw new HostedClientError('HOSTED_CONTRACT_MISMATCH', 'Webcmd Cloud manifest does not match this installed Webcmd hosted contract.', 'Upgrade Webcmd or use a compatible Webcmd Cloud endpoint.', EXIT_CODES.CONFIG_ERROR);
        }
        if (!hasExactKeys(body, ['ok', 'manifest']) || !isHostedManifest(body.manifest)) {
            throw protocolError('Webcmd Cloud returned an invalid manifest.');
        }
        return body.manifest;
    }
    async listProfiles() {
        const body = await this.request('/v1/profiles');
        if (!isHostedProfilesResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid profiles response.');
        }
        return body;
    }
    async validateAdapters(target) {
        const params = new URLSearchParams();
        if (target !== undefined)
            params.set('target', target);
        const body = await this.request(`/v1/adapters/validate${params.size ? `?${params}` : ''}`);
        if (!hasExactKeys(body, ['ok', 'report']) || body.ok !== true || !isValidationReport(body.report)) {
            throw protocolError('Webcmd Cloud returned an invalid adapter validation response.');
        }
        return body.report;
    }
    async verifyAdapters(input) {
        const body = await this.request('/v1/adapters/verify', {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!hasExactKeys(body, ['ok', 'report']) || body.ok !== true || !isHostedVerifyReport(body.report)) {
            throw protocolError('Webcmd Cloud returned an invalid adapter verification response.');
        }
        return body.report;
    }
    async auditAdapterConventions(input) {
        const params = new URLSearchParams();
        if (input.target !== undefined)
            params.set('target', input.target);
        if (input.site !== undefined)
            params.set('site', input.site);
        const body = await this.request(`/v1/adapters/convention-audit${params.size ? `?${params}` : ''}`);
        if (!hasExactKeys(body, ['ok', 'report']) || body.ok !== true || !isConventionAuditReport(body.report)) {
            throw protocolError('Webcmd Cloud returned an invalid adapter convention audit response.');
        }
        return body.report;
    }
    async getDoctor(profile) {
        const params = new URLSearchParams();
        if (profile !== undefined)
            params.set('profile', profile);
        const body = await this.request(`/v1/doctor${params.size ? `?${params}` : ''}`);
        if (!hasExactKeys(body, ['ok', 'report']) || body.ok !== true || !isHostedDoctorReport(body.report)) {
            throw protocolError('Webcmd Cloud returned an invalid doctor response.');
        }
        return body.report;
    }
    async listAdapters() {
        const body = await this.request('/v1/adapters/status');
        if (!hasExactKeys(body, ['ok', 'adapters']) || body.ok !== true
            || !Array.isArray(body.adapters) || !body.adapters.every(isHostedAdapterStatusRow)) {
            throw protocolError('Webcmd Cloud returned an invalid adapter status response.');
        }
        return body.adapters;
    }
    async resetAdapterOverrides(input) {
        const site = input.site?.trim();
        if ((!site && input.all !== true) || (site !== undefined && input.all === true)) {
            throw new ArgumentError('Specify one adapter site or --all.');
        }
        const params = new URLSearchParams(site ? { site } : { all: 'true' });
        const body = await this.request(`/v1/adapters/overrides?${params}`, { method: 'DELETE' });
        if (!hasExactKeys(body, ['ok', 'removed']) || body.ok !== true
            || !Array.isArray(body.removed) || !body.removed.every(isHostedAdapterResetRemoval)) {
            throw protocolError('Webcmd Cloud returned an invalid adapter reset response.');
        }
        return body.removed;
    }
    async createProfile(name) {
        const body = await this.request('/v1/profiles', {
            method: 'POST',
            body: JSON.stringify({ name }),
        }, undefined, 201);
        if (!isHostedProfileCreateResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid profile creation response.');
        }
        return body;
    }
    async renameProfile(profileId, name) {
        const body = await this.request(`/v1/profiles/${encodeURIComponent(profileId)}`, {
            method: 'PATCH',
            body: JSON.stringify({ name }),
        });
        if (!isHostedProfileRenameResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid profile rename response.');
        }
        return body;
    }
    async listMarketplaceCatalog() {
        const body = await this.request('/v1/marketplace/catalog');
        if (!isHostedMarketplaceCatalogResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace catalog response.');
        }
        return body;
    }
    async deleteProfile(profileId) {
        const body = await this.request(`/v1/profiles/${encodeURIComponent(profileId)}`, { method: 'DELETE' });
        if (!hasExactKeys(body, ['ok', 'deleted']) || body.ok !== true || body.deleted !== true) {
            throw protocolError('Webcmd Cloud returned an invalid profile deletion response.');
        }
        return { ok: true, deleted: true };
    }
    async createBrowserSession(name, profile) {
        const body = await this.request('/v1/sessions', {
            method: 'POST',
            body: JSON.stringify(profile !== undefined ? { name, profile } : { name }),
        });
        if (!isHostedBrowserSessionResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid browser session response.');
        }
        return body;
    }
    async listBrowserSessions(profile, limit) {
        const body = await this.request(`/v1/sessions${sessionListQuery(profile, limit)}`);
        if (!isHostedBrowserSessionsResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid browser session list.');
        }
        return body;
    }
    async closeBrowserSession(session, profile, force = false) {
        const body = await this.request(`/v1/sessions/${encodeURIComponent(session)}/close${profileQuery(profile)}`, {
            method: 'POST',
            body: JSON.stringify(force ? { force: true } : {}),
        });
        if (!isHostedBrowserSessionCloseResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid browser session close response.');
        }
        return body;
    }
    async searchMarketplacePlugins(query) {
        const params = new URLSearchParams();
        if (query !== undefined)
            params.set('query', query);
        const body = await this.request(`/v1/marketplace/plugins${params.size ? `?${params}` : ''}`);
        if (!hasExactKeys(body, ['ok', 'result']) || body.ok !== true || !isHostedMarketplaceSearchResult(body.result)) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace search response.');
        }
        return body.result;
    }
    async installMarketplacePlugin(installSource) {
        const body = await this.request('/v1/marketplace/installations', {
            method: 'POST',
            body: JSON.stringify({ installSource }),
        });
        if (!hasExactKeys(body, ['ok', 'result']) || body.ok !== true || !isHostedMarketplaceInstallation(body.result)) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace installation response.');
        }
        return body.result;
    }
    async listMarketplaceInstallations() {
        const body = await this.request('/v1/marketplace/installations');
        if (!hasExactKeys(body, ['ok', 'result']) || body.ok !== true
            || !isRecord(body.result) || !Array.isArray(body.result.installations)
            || !body.result.installations.every(isHostedMarketplaceInstallationRow)) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace installation list.');
        }
        return body.result.installations;
    }
    async uninstallMarketplacePlugin(name) {
        const body = await this.request(`/v1/marketplace/installations/${encodeURIComponent(name)}`, { method: 'DELETE' });
        if (!hasExactKeys(body, ['ok', 'result']) || body.ok !== true
            || !isRecord(body.result) || body.result.uninstalled !== true) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace uninstall response.');
        }
        return { uninstalled: true };
    }
    async updateMarketplacePlugin(name) {
        const body = await this.request(`/v1/marketplace/installations/${encodeURIComponent(name)}/update`, { method: 'POST' });
        if (!hasExactKeys(body, ['ok', 'result']) || body.ok !== true
            || !isHostedMarketplaceUpdateResult(body.result)) {
            throw protocolError('Webcmd Cloud returned an invalid marketplace update response.');
        }
        return body.result;
    }
    async listSiteMemory(site) {
        const body = await this.request(`/v1/sites/${encodeURIComponent(site)}/memory`);
        if (!isHostedSiteMemoryListResponse(body))
            throw protocolError('Webcmd Cloud returned an invalid site memory list.');
        return body.artifacts;
    }
    async readSiteMemory(site, path) {
        return this.requestText(`/v1/sites/${encodeURIComponent(site)}/memory/${encodePath(path)}`);
    }
    async writeSiteMemory(site, path, body, contentType = 'application/json') {
        const result = await this.request(`/v1/sites/${encodeURIComponent(site)}/memory/${encodePath(path)}`, {
            method: 'PUT', headers: { 'content-type': contentType }, body,
        });
        if (!isHostedOkResponse(result))
            throw protocolError('Webcmd Cloud returned an invalid site memory write response.');
    }
    async deleteSiteMemory(site, path, body) {
        const result = await this.request(`/v1/sites/${encodeURIComponent(site)}/memory/${encodePath(path)}`, {
            method: 'DELETE', ...(body === undefined ? {} : { headers: { 'content-type': 'application/json' }, body }),
        });
        if (!isHostedSiteMemoryDeletionResponse(result))
            throw protocolError('Webcmd Cloud returned an invalid site memory deletion response.');
    }
    async readAdapterSource(packageId, sourcePath) {
        return this.requestText(`/v1/adapters/${encodeURIComponent(packageId)}/source/${encodePath(sourcePath)}`);
    }
    async writeAdapterSource(packageId, sourcePath, body) {
        const result = await this.request(`/v1/adapters/${encodeURIComponent(packageId)}/source/${encodePath(sourcePath)}`, {
            method: 'PUT', headers: { 'content-type': 'text/javascript; charset=utf-8' }, body,
        });
        if (!isHostedAdapterSourceWriteResponse(result))
            throw protocolError('Webcmd Cloud returned an invalid adapter source write response.');
        return { packageId: result.package.id, storagePath: result.package.storagePath, commands: result.commands };
    }
    /** Hosted `adapter override`: fork an installed system command into a private package. */
    async overrideAdapter(commandKey) {
        const result = await this.request('/v1/adapters/override', {
            method: 'POST',
            headers: { 'content-type': 'application/json' },
            body: JSON.stringify({ command: commandKey }),
        });
        if (!isHostedAdapterOverrideResponse(result))
            throw protocolError('Webcmd Cloud returned an invalid adapter override response.');
        return {
            command: result.command,
            packageId: result.package.id,
            packageName: result.package.name,
            sourceFile: result.sourceFile,
        };
    }
    async execute(input) {
        const traceMode = normalizeTraceMode(input.trace);
        const body = await this.request('/v1/execute', {
            method: 'POST',
            body: JSON.stringify(input),
        }, { command: input.command, traceMode });
        if (!isHostedExecuteResponse(body, input.command, traceMode)) {
            throw protocolError('Webcmd Cloud returned an invalid execution response.');
        }
        return body;
    }
    async prepareExecution(input) {
        const body = await this.request('/v1/executions', {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedPrepareExecutionResponse(body, input.command)) {
            throw protocolError('Webcmd Cloud returned an invalid prepared execution response.');
        }
        return body;
    }
    async uploadExecutionArtifact(input) {
        const body = await this.request(`/v1/executions/${encodeURIComponent(input.executionId)}/artifacts/${encodeURIComponent(input.argument)}`, {
            method: 'POST',
            headers: {
                'content-type': 'application/octet-stream',
                'x-webcmd-filename': input.filename,
                'x-webcmd-content-type': input.contentType,
            },
            body: input.body,
        });
        if (!isHostedUploadArtifactResponse(body, input.argument)) {
            throw protocolError('Webcmd Cloud returned an invalid artifact upload response.');
        }
        return body;
    }
    async runPreparedExecution(input) {
        const traceMode = normalizeTraceMode(input.trace);
        const body = await this.request(`/v1/executions/${encodeURIComponent(input.executionId)}/run`, {
            method: 'POST',
            body: JSON.stringify({
                command: input.command,
                args: input.args,
                ...(input.format !== undefined ? { format: input.format } : {}),
                ...(input.trace !== undefined ? { trace: input.trace } : {}),
                ...(input.profile !== undefined ? { profile: input.profile } : {}),
                ...(input.session !== undefined ? { session: input.session } : {}),
            }),
        }, { command: input.command, traceMode });
        if (!isHostedExecuteResponse(body, input.command, traceMode)) {
            throw protocolError('Webcmd Cloud returned an invalid execution response.');
        }
        return body;
    }
    async downloadExecutionArtifact(input) {
        const response = await this.authenticatedFetch(`/v1/executions/${encodeURIComponent(input.executionId)}/artifacts/${encodeURIComponent(input.artifactId)}`, { headers: { accept: 'application/octet-stream' } });
        if (!response.ok) {
            const text = await response.text();
            const body = text ? parseJson(text) : {};
            if (!isHostedError(body))
                throw protocolError('Webcmd Cloud returned an invalid artifact download failure.');
            throw hostedFailure(body, response.status === 401 ? EXIT_CODES.NOPERM : EXIT_CODES.GENERIC_ERROR);
        }
        return new Uint8Array(await response.arrayBuffer());
    }
    async downloadArtifactFromUrl(downloadUrl) {
        const ids = parseExecutionArtifactDownloadUrl(downloadUrl, this.apiBaseUrl);
        return this.downloadExecutionArtifact(ids);
    }
    async startBrowserRun(session, input) {
        const body = await this.request(`/v1/browser/${encodeURIComponent(session)}/runs`, {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedBrowserRunResponse(body, session)) {
            throw protocolError('Webcmd Cloud returned an invalid browser run response.');
        }
        return body;
    }
    async browserAction(session, executionId, input) {
        const body = await this.request(`/v1/browser/${encodeURIComponent(session)}/runs/${encodeURIComponent(executionId)}/actions`, {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedBrowserActionResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid browser action response.');
        }
        return body;
    }
    async finishBrowserRun(session, executionId, input) {
        const body = await this.request(`/v1/browser/${encodeURIComponent(session)}/runs/${encodeURIComponent(executionId)}/finish`, {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedBrowserFinishResponse(body, executionId, input.status)) {
            throw protocolError('Webcmd Cloud returned an invalid browser finish response.');
        }
        return body;
    }
    async runBrowserAction(session, input) {
        return this.executeBrowserCommand(session, input);
    }
    async executeBrowserCommand(session, input) {
        const body = await this.request(`/v1/browser/${encodeURIComponent(session)}/commands`, {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedBrowserRunActionResponse(body, session) && !(input.action === 'snapshot' && isHostedBrowserSnapshotActionResponse(body, session))) {
            throw protocolError('Webcmd Cloud returned an invalid browser action response.');
        }
        return body;
    }
    async executeAuthoringCommand(input) {
        const body = await this.request('/v1/browser/authoring/commands', {
            method: 'POST',
            body: JSON.stringify(input),
        });
        if (!isHostedAuthoringCommandResponse(body)) {
            throw protocolError('Webcmd Cloud returned an invalid authoring response.');
        }
        return body;
    }
    async request(path, init = {}, executionExpectation, expectedStatus) {
        const response = await this.authenticatedFetch(path, init);
        const text = await response.text();
        const body = text ? parseJson(text) : {};
        if (!isRecord(body) || (body.ok !== true && body.ok !== false)) {
            throw protocolError('Webcmd Cloud returned an invalid response envelope.');
        }
        if (body.ok === false) {
            if (!isHostedError(body))
                throw protocolError('Webcmd Cloud returned an invalid failure response.');
            if (body.execution && !isValidExecutedFailure(body, executionExpectation)) {
                throw protocolError('Webcmd Cloud returned an invalid executed failure response.');
            }
            throw hostedFailure(body, response.status === 401 ? EXIT_CODES.NOPERM : EXIT_CODES.GENERIC_ERROR);
        }
        if (!response.ok)
            throw protocolError('Webcmd Cloud returned a success envelope with an HTTP error status.');
        if (expectedStatus !== undefined && response.status !== expectedStatus) {
            throw protocolError(`Webcmd Cloud returned HTTP ${response.status}; expected ${expectedStatus}.`);
        }
        return body;
    }
    async authenticatedFetch(path, init = {}) {
        // Verbose diagnostics deliberately carry the method, path, status and elapsed
        // time only. The bearer token, the workspace header and request/response
        // bodies are never logged — `-v` is a debugging aid, not a credential dump.
        const method = init.method ?? 'GET';
        const startedAt = Date.now();
        log.verbose(`hosted → ${method} ${path}`);
        try {
            const signal = mergeAbortSignals(this.signal, init.signal);
            const response = await this.fetchImpl(`${this.apiBaseUrl}${path}`, {
                ...init,
                ...(signal ? { signal } : {}),
                headers: {
                    accept: 'application/json',
                    authorization: `Bearer ${this.apiKey}`,
                    'x-webcmd-session-protocol-version': String(HOSTED_SESSION_PROTOCOL_VERSION),
                    'x-webcmd-client-capabilities': [
                        'hosted-live-view-v1',
                        HOSTED_CORE_COMMANDS_CAPABILITY,
                        HOSTED_COMMAND_ORIGIN_CAPABILITY,
                    ].join(', '),
                    ...(init.body ? { 'content-type': 'application/json' } : {}),
                    ...(this.workspace ? { 'x-webcmd-workspace': this.workspace } : {}),
                    ...(init.headers ?? {}),
                },
            });
            log.verbose(`hosted ← ${method} ${path} ${response.status} (${Date.now() - startedAt}ms)`);
            return response;
        }
        catch (error) {
            log.verbose(`hosted ✖ ${method} ${path} failed after ${Date.now() - startedAt}ms: ` +
                `${error instanceof Error ? error.message : String(error)}`);
            throw error;
        }
    }
    async requestText(path) {
        const response = await this.authenticatedFetch(path, { headers: { accept: 'text/plain, application/json' } });
        const text = await response.text();
        if (response.ok)
            return text;
        const body = text ? parseJson(text) : {};
        if (!isHostedError(body))
            throw protocolError('Webcmd Cloud returned an invalid raw response failure.');
        throw hostedFailure(body, response.status === 401 ? EXIT_CODES.NOPERM : EXIT_CODES.GENERIC_ERROR);
    }
}
function mergeAbortSignals(base, perRequest) {
    if (!base)
        return perRequest ?? undefined;
    if (!perRequest)
        return base;
    return AbortSignal.any([base, perRequest]);
}
function parseJson(text) {
    try {
        return JSON.parse(text);
    }
    catch {
        throw protocolError('Webcmd Cloud returned non-JSON response.');
    }
}
function encodePath(value) {
    const parts = value.split('/');
    if (!value || value.includes('\\') || parts.some(part => !part || part === '.' || part === '..')) {
        throw protocolError('Webcmd Cloud received an invalid storage path.');
    }
    return parts.map(encodeURIComponent).join('/');
}
function isHostedOkResponse(value) {
    return hasExactKeys(value, ['ok']) && value.ok === true;
}
function isHostedSiteMemoryDeletionResponse(value) {
    return isHostedOkResponse(value)
        || (hasExactKeys(value, ['ok', 'stale']) && value.ok === true && value.stale === true)
        || (hasExactKeys(value, ['ok', 'deleted']) && value.ok === true && value.deleted === true);
}
function isHostedSiteMemoryListResponse(value) {
    return hasExactKeys(value, ['ok', 'artifacts'])
        && value.ok === true
        && Array.isArray(value.artifacts)
        && value.artifacts.every(artifact => isRecord(artifact)
            && hasExactKeys(artifact, ['path', 'kind', 'contentType', 'sha256', 'byteSize', 'updatedAt'])
            && typeof artifact.path === 'string' && typeof artifact.kind === 'string' && typeof artifact.contentType === 'string'
            && typeof artifact.sha256 === 'string' && typeof artifact.byteSize === 'number' && typeof artifact.updatedAt === 'string');
}
function isHostedAdapterSourceWriteResponse(value) {
    return hasExactKeys(value, ['ok', 'package', 'commands'])
        && value.ok === true
        && isRecord(value.package)
        && hasExactKeys(value.package, ['id', 'storagePath'])
        && typeof value.package.id === 'string'
        && typeof value.package.storagePath === 'string'
        && Array.isArray(value.commands)
        && value.commands.every(command => typeof command === 'string');
}
function isHostedAdapterOverrideResponse(value) {
    return hasExactKeys(value, ['ok', 'command', 'package', 'installation', 'sourceFile'])
        && value.ok === true
        && typeof value.command === 'string'
        && isRecord(value.package)
        && hasExactKeys(value.package, ['id', 'name', 'visibility'])
        && typeof value.package.id === 'string'
        && typeof value.package.name === 'string'
        && typeof value.package.visibility === 'string'
        && isRecord(value.installation)
        && hasExactKeys(value.installation, ['id'])
        && typeof value.installation.id === 'string'
        && (value.sourceFile === null || typeof value.sourceFile === 'string');
}
function isHostedError(value) {
    if (!hasOnlyKeys(value, ['ok', 'error', 'execution', 'trace']) || value.ok !== false || !isRecord(value.error))
        return false;
    if (!hasOnlyKeys(value.error, ['code', 'message', 'help', 'exitCode', 'details']))
        return false;
    if (typeof value.error.code !== 'string' || typeof value.error.message !== 'string')
        return false;
    if (value.error.exitCode !== undefined
        && (typeof value.error.exitCode !== 'number' || !isAllowedExitCode(value.error.exitCode)))
        return false;
    if (value.error.help !== undefined && typeof value.error.help !== 'string')
        return false;
    if (value.error.details !== undefined && !isRecord(value.error.details))
        return false;
    if (value.execution !== undefined && !isHostedExecution(value.execution))
        return false;
    if (value.trace !== undefined && !isHostedTraceReceipt(value.trace))
        return false;
    if (value.execution?.status === 'succeeded')
        return false;
    if (value.trace && (!value.execution || value.trace.executionId !== value.execution.id))
        return false;
    return true;
}
function isHostedManifest(value) {
    if (!hasExactKeys(value, ['userId', 'metadata', 'commands'])
        || typeof value.userId !== 'string'
        || !hasOnlyKeys(value.metadata, [
            'contractSchemaVersion',
            'sessionProtocolVersion',
            'webcmdPackageVersion',
            'generatedAt',
            'coreCommands',
        ])
        || typeof value.metadata.contractSchemaVersion !== 'number'
        || !Number.isInteger(value.metadata.contractSchemaVersion)
        || value.metadata.contractSchemaVersion <= 0
        || typeof value.metadata.sessionProtocolVersion !== 'number'
        || !Number.isInteger(value.metadata.sessionProtocolVersion)
        || value.metadata.sessionProtocolVersion <= 0
        || typeof value.metadata.webcmdPackageVersion !== 'string'
        || typeof value.metadata.generatedAt !== 'string'
        || !Array.isArray(value.commands)
        || !value.commands.every(isHostedManifestCommand))
        return false;
    const coreCommands = value.metadata.coreCommands;
    if (coreCommands !== undefined) {
        if (!Array.isArray(coreCommands) || !coreCommands.every(isHostedCoreCommandId))
            return false;
        if (new Set(coreCommands).size !== coreCommands.length)
            return false;
    }
    return true;
}
function isHostedExecuteResponse(value, requestedCommand, traceMode) {
    if (!hasOnlyKeys(value, ['ok', 'result', 'columns', 'footerExtra', 'execution', 'trace', 'artifacts'])
        || value.ok !== true
        || !Object.prototype.hasOwnProperty.call(value, 'result'))
        return false;
    if (!isHostedExecution(value.execution) || value.execution.status !== 'succeeded')
        return false;
    if (value.execution.command !== requestedCommand)
        return false;
    if (value.columns !== undefined && (!Array.isArray(value.columns) || !value.columns.every(column => typeof column === 'string'))) {
        return false;
    }
    if (value.footerExtra !== undefined && typeof value.footerExtra !== 'string')
        return false;
    if (value.artifacts !== undefined && (!Array.isArray(value.artifacts) || !value.artifacts.every(isHostedArtifactReceipt)))
        return false;
    if (value.trace !== undefined && !isHostedTraceReceipt(value.trace))
        return false;
    if (value.trace && value.trace.executionId !== value.execution.id)
        return false;
    if (traceMode === 'on' ? !value.trace : value.trace !== undefined)
        return false;
    return true;
}
function isHostedPrepareExecutionResponse(value, requestedCommand) {
    return hasOnlyKeys(value, ['ok', 'execution', 'fileArguments', 'liveViewUrl'])
        && value.ok === true
        && hasExactKeys(value.execution, ['id', 'command', 'status'])
        && typeof value.execution.id === 'string'
        && value.execution.command === requestedCommand
        && value.execution.status === 'queued'
        && Array.isArray(value.fileArguments)
        && value.fileArguments.every(isHostedFileArgument)
        && (value.liveViewUrl === undefined || typeof value.liveViewUrl === 'string');
}
function isHostedUploadArtifactResponse(value, argument) {
    if (!hasExactKeys(value, ['ok', 'artifact', 'reference']) || value.ok !== true)
        return false;
    const artifact = value.artifact;
    if (!isHostedArtifactReceipt(artifact))
        return false;
    if (artifact.argument !== argument)
        return false;
    if (!hasExactKeys(value.reference, ['$webcmdArtifact']))
        return false;
    const reference = value.reference.$webcmdArtifact;
    return hasOnlyKeys(reference, ['id', 'direction', 'filename', 'contentType'])
        && typeof reference.id === 'string'
        && (reference.direction === undefined || reference.direction === 'input');
}
function isHostedProfilesResponse(value) {
    return hasExactKeys(value, ['ok', 'profiles'])
        && value.ok === true
        && Array.isArray(value.profiles)
        && value.profiles.every(isHostedPublicProfile);
}
function isValidationReport(value) {
    return hasExactKeys(value, ['ok', 'results', 'errors', 'warnings', 'commands'])
        && typeof value.ok === 'boolean'
        && Array.isArray(value.results)
        && value.results.every(result => hasExactKeys(result, ['label', 'errors', 'warnings'])
            && typeof result.label === 'string'
            && Array.isArray(result.errors)
            && result.errors.every(error => typeof error === 'string')
            && Array.isArray(result.warnings)
            && result.warnings.every(warning => typeof warning === 'string'))
        && isNonNegativeInteger(value.errors)
        && isNonNegativeInteger(value.warnings)
        && isNonNegativeInteger(value.commands);
}
const CONVENTION_RULE_IDS = new Set([
    'silent-column-drop',
    'camelCase-in-columns',
    'missing-access-metadata',
    'silent-clamp',
    'silent-empty-fallback',
    'silent-sentinel',
    'write-without-delete-pair',
]);
function isConventionAuditReport(value) {
    return hasExactKeys(value, ['ok', 'summary', 'categories'])
        && typeof value.ok === 'boolean'
        && hasExactKeys(value.summary, ['commands', 'sites', 'files_scanned', 'violations'])
        && isNonNegativeInteger(value.summary.commands)
        && isNonNegativeInteger(value.summary.sites)
        && isNonNegativeInteger(value.summary.files_scanned)
        && isNonNegativeInteger(value.summary.violations)
        && Array.isArray(value.categories)
        && value.categories.every(category => hasExactKeys(category, ['rule', 'count', 'violations'])
            && isConventionRuleId(category.rule)
            && isNonNegativeInteger(category.count)
            && Array.isArray(category.violations)
            && category.violations.every(isConventionViolation));
}
function isConventionViolation(value) {
    if (!isRecord(value))
        return false;
    const expected = [
        'rule', 'site', 'name', 'command', 'message',
        ...(value.file === undefined ? [] : ['file']),
        ...(value.line === undefined ? [] : ['line']),
        ...(value.details === undefined ? [] : ['details']),
    ];
    return hasExactKeys(value, expected)
        && isConventionRuleId(value.rule)
        && typeof value.site === 'string'
        && typeof value.name === 'string'
        && typeof value.command === 'string'
        && typeof value.message === 'string'
        && (value.file === undefined || typeof value.file === 'string')
        && (value.line === undefined || isNonNegativeInteger(value.line))
        && (value.details === undefined || isRecord(value.details));
}
function isConventionRuleId(value) {
    return typeof value === 'string' && CONVENTION_RULE_IDS.has(value);
}
function isHostedVerifyReport(value) {
    return hasExactKeys(value, ['ok', 'validation', 'smoke'])
        && typeof value.ok === 'boolean'
        && isValidationReport(value.validation)
        && (value.smoke === null || isHostedVerifySmoke(value.smoke));
}
function isHostedVerifySmoke(value) {
    return hasExactKeys(value, ['requested', 'executed', 'ok', 'summary', 'results'])
        && value.requested === true
        && typeof value.executed === 'boolean'
        && typeof value.ok === 'boolean'
        && typeof value.summary === 'string'
        && Array.isArray(value.results)
        && value.results.every(isHostedVerifySmokeResult);
}
function isHostedVerifySmokeResult(value) {
    return hasExactKeys(value, ['command', 'status', 'message'])
        && typeof value.command === 'string'
        && (value.status === 'passed' || value.status === 'failed' || value.status === 'skipped')
        && typeof value.message === 'string';
}
const HOSTED_DOCTOR_CHECK_IDS = new Set([
    'api',
    'workspace',
    'compatibility',
    'profile',
    'browser-provider',
    'capacity',
]);
function isHostedDoctorReport(value) {
    return hasExactKeys(value, ['ok', 'checks'])
        && typeof value.ok === 'boolean'
        && Array.isArray(value.checks)
        && value.checks.every(check => hasExactKeys(check, ['id', 'ok', 'required', 'message'])
            && typeof check.id === 'string'
            && HOSTED_DOCTOR_CHECK_IDS.has(check.id)
            && typeof check.ok === 'boolean'
            && typeof check.required === 'boolean'
            && typeof check.message === 'string');
}
function isHostedAdapterStatusRow(value) {
    return hasExactKeys(value, ['command', 'kind', 'package', 'reconciliationState', 'loadError'])
        && typeof value.command === 'string'
        && (value.kind === 'override' || value.kind === 'user')
        && typeof value.package === 'string'
        && (value.reconciliationState === 'current'
            || value.reconciliationState === 'changed'
            || value.reconciliationState === 'unknown')
        && (value.loadError === null || typeof value.loadError === 'string');
}
function isHostedAdapterResetRemoval(value) {
    return hasExactKeys(value, ['packageId', 'package', 'commands'])
        && typeof value.packageId === 'string'
        && typeof value.package === 'string'
        && Array.isArray(value.commands)
        && value.commands.every(command => typeof command === 'string');
}
function isHostedProfileCreateResponse(value) {
    return hasExactKeys(value, ['ok', 'profile', 'created'])
        && value.ok === true
        && isHostedPublicProfile(value.profile)
        && typeof value.created === 'boolean';
}
function isHostedProfileRenameResponse(value) {
    return hasExactKeys(value, ['ok', 'profile', 'changed'])
        && value.ok === true
        && isHostedPublicProfile(value.profile)
        && typeof value.changed === 'boolean';
}
function isHostedMarketplaceCatalogResponse(value) {
    return hasExactKeys(value, ['ok', 'sources'])
        && value.ok === true
        && Array.isArray(value.sources)
        && value.sources.every(isHostedMarketplaceCatalogSource);
}
function isHostedMarketplaceCatalogSource(value) {
    return hasOnlyKeys(value, ['id', 'repository', 'commit', 'manifestPath', 'status'])
        && typeof value.id === 'string'
        && typeof value.repository === 'string'
        && (value.commit === undefined || typeof value.commit === 'string')
        && typeof value.manifestPath === 'string'
        && (value.status === 'empty' || value.status === 'consistent' || value.status === 'mixed');
}
function isNonNegativeInteger(value) {
    return typeof value === 'number' && Number.isInteger(value) && value >= 0;
}
function isHostedBrowserSessionResponse(value) {
    return hasExactKeys(value, ['ok', 'session'])
        && value.ok === true
        && isHostedCreatedBrowserSession(value.session);
}
function isHostedBrowserSessionsResponse(value) {
    return hasExactKeys(value, ['ok', 'sessions'])
        && value.ok === true
        && Array.isArray(value.sessions)
        && value.sessions.every(isHostedBrowserSession);
}
function isHostedBrowserSessionCloseResponse(value) {
    return hasOnlyKeys(value, ['ok', 'closed', 'alreadyIdle', 'session', 'displaced'])
        && value.ok === true
        && typeof value.closed === 'boolean'
        && typeof value.alreadyIdle === 'boolean'
        && typeof value.session === 'string'
        && (value.displaced === undefined || isHostedSessionDisplacement(value.displaced));
}
function isHostedBrowserSession(value) {
    return hasExactKeys(value, ['id', 'kind', 'profileId', 'runtimeState', 'handoff', 'createdAt', 'updatedAt', 'lastUsedAt'])
        && typeof value.id === 'string'
        && (value.kind === 'explicit' || value.kind === 'adapter-default')
        && typeof value.profileId === 'string'
        && (value.runtimeState === 'active' || value.runtimeState === 'idle')
        && isHostedSessionHandoff(value.handoff)
        && typeof value.createdAt === 'string'
        && typeof value.updatedAt === 'string'
        && typeof value.lastUsedAt === 'string';
}
function isHostedCreatedBrowserSession(value) {
    if (!isRecord(value) || typeof value.liveViewUrl !== 'string')
        return false;
    const { liveViewUrl: _liveViewUrl, ...session } = value;
    return isHostedBrowserSession(session);
}
function isHostedSessionHandoff(value) {
    return value === null
        || (hasExactKeys(value, ['site', 'expiresAt'])
            && typeof value.site === 'string'
            && typeof value.expiresAt === 'string');
}
function isHostedSessionDisplacement(value) {
    return hasOnlyKeys(value, ['executionId', 'handoffSite'])
        && (value.executionId === undefined || typeof value.executionId === 'string')
        && (value.handoffSite === undefined || typeof value.handoffSite === 'string');
}
function profileQuery(profile) {
    if (profile === undefined)
        return '';
    const params = new URLSearchParams({ profile });
    return `?${params}`;
}
function sessionListQuery(profile, limit) {
    const params = new URLSearchParams();
    if (profile !== undefined)
        params.set('profile', profile);
    if (limit !== undefined)
        params.set('limit', String(limit));
    return params.size ? `?${params}` : '';
}
function isHostedMarketplaceSearchResult(value) {
    return hasExactKeys(value, ['plugins', 'errors'])
        && Array.isArray(value.plugins)
        && value.plugins.every(isHostedMarketplacePlugin)
        && Array.isArray(value.errors)
        && value.errors.every(isHostedMarketplaceSearchError);
}
function isHostedMarketplacePlugin(value) {
    return hasOnlyKeys(value, ['name', 'description', 'version', 'sourceId', 'installSource', 'webcmd', 'availability', 'excludedCommands'])
        && typeof value.name === 'string'
        && typeof value.sourceId === 'string'
        && typeof value.installSource === 'string'
        && (value.availability === 'hosted' || value.availability === 'mixed' || value.availability === 'local-only')
        && Array.isArray(value.excludedCommands)
        && value.excludedCommands.every(command => typeof command === 'string')
        && (value.description === undefined || typeof value.description === 'string')
        && (value.version === undefined || typeof value.version === 'string')
        && (value.webcmd === undefined || typeof value.webcmd === 'string');
}
function isHostedMarketplaceSearchError(value) {
    return hasExactKeys(value, ['sourceId', 'manifestUrl', 'message'])
        && typeof value.sourceId === 'string'
        && typeof value.manifestUrl === 'string'
        && typeof value.message === 'string';
}
function isHostedMarketplaceInstallation(value) {
    return hasExactKeys(value, ['installationId', 'name', 'version', 'installSource'])
        && typeof value.installationId === 'string'
        && typeof value.name === 'string'
        && typeof value.version === 'string'
        && typeof value.installSource === 'string';
}
function isHostedMarketplaceInstallationRow(value) {
    return hasExactKeys(value, ['name', 'version', 'installSource', 'sourceCommit', 'installedAt', 'updateAvailable'])
        && typeof value.name === 'string'
        && typeof value.version === 'string'
        && typeof value.installSource === 'string'
        && (value.sourceCommit === null || typeof value.sourceCommit === 'string')
        && typeof value.installedAt === 'string'
        && typeof value.updateAvailable === 'boolean';
}
function isHostedMarketplaceUpdateResult(value) {
    return hasOnlyKeys(value, ['updated', 'name', 'version', 'delisted'])
        && typeof value.updated === 'boolean'
        && typeof value.name === 'string'
        && typeof value.version === 'string'
        && (value.delisted === undefined || value.delisted === true);
}
function isHostedPublicProfile(value) {
    return hasExactKeys(value, [
        'id', 'name', 'workspace', 'default', 'status',
        'createdAt', 'updatedAt', 'lastUsedAt',
    ])
        && typeof value.id === 'string'
        && (value.name === null || typeof value.name === 'string')
        && (value.workspace === null || typeof value.workspace === 'string')
        && typeof value.default === 'boolean'
        && (value.status === 'pending' || value.status === 'available')
        && typeof value.createdAt === 'string'
        && typeof value.updatedAt === 'string'
        && typeof value.lastUsedAt === 'string';
}
function isHostedManifestCommand(value) {
    if (!hasOnlyKeys(value, [
        'site', 'name', 'aliases', 'command', 'description', 'access', 'example', 'domain', 'strategy', 'browser',
        'args', 'columns', 'tags', 'keywords', 'pipeline', 'defaultFormat', 'type', 'modulePath', 'sourceFile', 'navigateBefore',
        'siteSession', 'freshPage', 'adapterPackageId', 'adapterPackageName', 'adapterPackageVersion', 'origin',
    ]))
        return false;
    if (typeof value.site !== 'string' || typeof value.name !== 'string' || typeof value.command !== 'string')
        return false;
    if (typeof value.description !== 'string' || typeof value.access !== 'string' || typeof value.strategy !== 'string')
        return false;
    if (typeof value.browser !== 'boolean' || !Array.isArray(value.args) || !value.args.every(isHostedManifestArg))
        return false;
    if (value.aliases !== undefined && (!Array.isArray(value.aliases) || !value.aliases.every(item => typeof item === 'string')))
        return false;
    if (!Array.isArray(value.columns) || !value.columns.every(item => typeof item === 'string'))
        return false;
    if (value.tags !== undefined && (!Array.isArray(value.tags) || !value.tags.every(item => typeof item === 'string')))
        return false;
    if (value.keywords !== undefined && (!Array.isArray(value.keywords) || !value.keywords.every(item => typeof item === 'string')))
        return false;
    if (value.domain !== undefined && value.domain !== null && typeof value.domain !== 'string')
        return false;
    if (value.defaultFormat !== undefined && value.defaultFormat !== null && typeof value.defaultFormat !== 'string')
        return false;
    if (value.example !== undefined && typeof value.example !== 'string')
        return false;
    if (value.pipeline !== undefined && (!Array.isArray(value.pipeline) || !value.pipeline.every(isRecord)))
        return false;
    for (const key of ['type', 'modulePath', 'sourceFile', 'siteSession', 'adapterPackageId', 'adapterPackageName', 'adapterPackageVersion', 'origin']) {
        if (value[key] !== undefined && typeof value[key] !== 'string')
            return false;
    }
    if (value.freshPage !== undefined && typeof value.freshPage !== 'boolean')
        return false;
    return value.navigateBefore === undefined || typeof value.navigateBefore === 'boolean' || typeof value.navigateBefore === 'string';
}
function isHostedManifestArg(value) {
    if (!hasOnlyKeys(value, ['name', 'type', 'required', 'default', 'valueRequired', 'positional', 'help', 'choices', 'file']))
        return false;
    if (typeof value.name !== 'string')
        return false;
    if (value.type !== undefined && typeof value.type !== 'string')
        return false;
    for (const key of ['required', 'valueRequired', 'positional']) {
        if (value[key] !== undefined && typeof value[key] !== 'boolean')
            return false;
    }
    if (value.help !== undefined && typeof value.help !== 'string')
        return false;
    if (value.file !== undefined && !isHostedArgFileMetadata(value.file))
        return false;
    return value.choices === undefined
        || (Array.isArray(value.choices) && value.choices.every(choice => typeof choice === 'string'));
}
function isHostedArgFileMetadata(value) {
    return hasOnlyKeys(value, ['direction', 'pathKind', 'multiple', 'separator', 'contentTypes', 'contentType', 'maxBytes', 'defaultPath'])
        && (value.direction === 'input' || value.direction === 'output' || value.direction === 'input-output')
        && (value.pathKind === 'file' || value.pathKind === 'directory')
        && typeof value.multiple === 'boolean'
        && (value.separator === undefined || value.separator === ',')
        && (value.contentTypes === undefined || (Array.isArray(value.contentTypes) && value.contentTypes.every(item => typeof item === 'string')))
        && (value.contentType === undefined || typeof value.contentType === 'string')
        && (value.maxBytes === undefined || (typeof value.maxBytes === 'number' && Number.isFinite(value.maxBytes) && value.maxBytes > 0))
        && (value.defaultPath === undefined || typeof value.defaultPath === 'string');
}
function isHostedFileArgument(value) {
    return hasOnlyKeys(value, ['name', 'direction', 'pathKind', 'multiple', 'required', 'separator', 'contentTypes', 'contentType', 'maxBytes', 'defaultPath'])
        && typeof value.name === 'string'
        && (value.direction === 'input' || value.direction === 'output' || value.direction === 'input-output')
        && (value.pathKind === 'file' || value.pathKind === 'directory')
        && typeof value.multiple === 'boolean'
        && typeof value.required === 'boolean'
        && (value.separator === undefined || value.separator === ',')
        && (value.contentTypes === undefined || (Array.isArray(value.contentTypes) && value.contentTypes.every(item => typeof item === 'string')))
        && (value.contentType === undefined || typeof value.contentType === 'string')
        && (value.maxBytes === undefined || (typeof value.maxBytes === 'number' && Number.isFinite(value.maxBytes) && value.maxBytes > 0))
        && (value.defaultPath === undefined || typeof value.defaultPath === 'string');
}
function isHostedArtifactReceipt(value) {
    return hasOnlyKeys(value, [
        'artifactId', 'argument', 'direction', 'pathKind', 'filename', 'contentType',
        'byteSize', 'sha256', 'relativePath', 'expiresAt',
    ])
        && typeof value.artifactId === 'string'
        && typeof value.argument === 'string'
        && (value.direction === 'input' || value.direction === 'output')
        && (value.pathKind === 'file' || value.pathKind === 'directory')
        && typeof value.filename === 'string'
        && typeof value.contentType === 'string'
        && typeof value.byteSize === 'number'
        && Number.isInteger(value.byteSize)
        && value.byteSize >= 0
        && (value.sha256 === undefined || typeof value.sha256 === 'string')
        && (value.relativePath === undefined || isSafeRelativeArtifactPath(value.relativePath))
        && typeof value.expiresAt === 'string';
}
function isSafeRelativeArtifactPath(value) {
    return typeof value === 'string'
        && value.length > 0
        && !value.startsWith('/')
        && !value.includes('\\')
        && !value.split('/').some(segment => !segment || segment === '.' || segment === '..' || segment.includes('\0'));
}
function isHostedBrowserRunResponse(value, requestedSession) {
    return hasExactKeys(value, ['ok', 'run']) && value.ok === true && isHostedBrowserRunPayload(value.run, requestedSession);
}
function isHostedBrowserRunPayload(value, requestedSession) {
    const run = value;
    if (!hasOnlyKeys(run, ['executionId', 'session', 'profile', 'liveViewUrl', 'expiresAt']))
        return false;
    if (typeof run.executionId !== 'string' || run.session !== requestedSession)
        return false;
    if (!hasExactKeys(run.profile, ['id', 'displayName']))
        return false;
    if (typeof run.profile.id !== 'string' || typeof run.profile.displayName !== 'string')
        return false;
    if (run.liveViewUrl !== undefined && typeof run.liveViewUrl !== 'string')
        return false;
    return run.expiresAt === undefined || typeof run.expiresAt === 'string';
}
function isHostedBrowserActionResponse(value) {
    if (!hasExactKeys(value, ['ok', 'result', 'columns', 'trace']) || value.ok !== true)
        return false;
    if (!Array.isArray(value.columns) || !value.columns.every(column => typeof column === 'string'))
        return false;
    return value.trace === null || isHostedBrowserActionTrace(value.trace);
}
function isHostedAuthoringCommandResponse(value) {
    if (!hasExactKeys(value, ['ok', 'result', 'columns', 'trace', 'run', 'execution']) || value.ok !== true)
        return false;
    if (!Array.isArray(value.columns) || !value.columns.every(column => typeof column === 'string'))
        return false;
    if (value.trace !== null && !isHostedBrowserActionTrace(value.trace))
        return false;
    if (!isHostedAuthoringRunPayload(value.run))
        return false;
    return hasExactKeys(value.execution, ['id', 'status'])
        && typeof value.execution.id === 'string'
        && value.execution.id === value.run.executionId
        && (value.execution.status === 'succeeded' || value.execution.status === 'failed' || value.execution.status === 'timed_out');
}
function isHostedAuthoringRunPayload(value) {
    if (!hasOnlyKeys(value, ['executionId', 'profile', 'liveViewUrl', 'expiresAt']))
        return false;
    if (typeof value.executionId !== 'string')
        return false;
    if (!hasExactKeys(value.profile, ['id', 'displayName']))
        return false;
    if (typeof value.profile.id !== 'string' || typeof value.profile.displayName !== 'string')
        return false;
    if (value.liveViewUrl !== undefined && typeof value.liveViewUrl !== 'string')
        return false;
    return value.expiresAt === undefined || typeof value.expiresAt === 'string';
}
function isHostedBrowserRunActionResponse(value, requestedSession) {
    if (!hasExactKeys(value, ['ok', 'result', 'columns', 'trace', 'run', 'execution']) || value.ok !== true)
        return false;
    if (!Array.isArray(value.columns) || !value.columns.every(column => typeof column === 'string'))
        return false;
    if (value.trace !== null && !isHostedBrowserActionTrace(value.trace))
        return false;
    if (!isHostedBrowserRunPayload(value.run, requestedSession))
        return false;
    return hasExactKeys(value.execution, ['id', 'status'])
        && typeof value.execution.id === 'string'
        && value.execution.id === value.run.executionId
        && (value.execution.status === 'succeeded' || value.execution.status === 'failed' || value.execution.status === 'timed_out');
}
function isHostedBrowserSnapshotActionResponse(value, requestedSession) {
    return hasExactKeys(value, ['ok', 'run', 'result'])
        && value.ok === true
        && isHostedBrowserRunPayload(value.run, requestedSession);
}
function isHostedBrowserActionTrace(value) {
    if (!hasOnlyKeys(value, ['id', 'receipt', 'kind', 'contentType', 'byteSize', 'storagePath']))
        return false;
    if (typeof value.id !== 'string' || typeof value.receipt !== 'string' || typeof value.kind !== 'string')
        return false;
    if (value.contentType !== undefined && typeof value.contentType !== 'string')
        return false;
    if (value.byteSize !== undefined
        && (typeof value.byteSize !== 'number' || !Number.isInteger(value.byteSize) || value.byteSize < 0))
        return false;
    return value.storagePath === undefined || typeof value.storagePath === 'string';
}
function isHostedBrowserFinishResponse(value, executionId, status) {
    return hasExactKeys(value, ['ok', 'execution'])
        && value.ok === true
        && hasExactKeys(value.execution, ['id', 'status'])
        && value.execution.id === executionId
        && value.execution.status === status;
}
function isHostedExecution(value) {
    return hasExactKeys(value, ['id', 'command', 'status'])
        && typeof value.id === 'string'
        && typeof value.command === 'string'
        && (value.status === 'succeeded' || value.status === 'failed' || value.status === 'timed_out');
}
function isHostedTraceReceipt(value) {
    if (!hasOnlyKeys(value, ['receipt', 'executionId', 'artifactsUrl', 'liveViewUrl', 'replayUrl'])
        || !isSafeReceiptToken(value.receipt)
        || !isSafeReceiptToken(value.executionId))
        return false;
    const executionBase = publicExecutionBase(value.executionId);
    if (!executionBase)
        return false;
    return optionalExactPath(value.artifactsUrl, `${executionBase}/artifacts`)
        && optionalExactPath(value.liveViewUrl, `${executionBase}/live`)
        && optionalExactPath(value.replayUrl, `${executionBase}/replay`);
}
function publicExecutionBase(executionId) {
    try {
        const encoded = encodeURIComponent(executionId);
        if (encoded === '.' || encoded === '..')
            return undefined;
        return `/v1/executions/${encoded}`;
    }
    catch {
        return undefined;
    }
}
function optionalExactPath(value, expected) {
    return value === undefined || value === expected;
}
function isSafeReceiptToken(value) {
    return typeof value === 'string'
        && value.length > 0
        && !/[\u0000-\u001f\u007f\u2028\u2029]/u.test(value);
}
function isRecord(value) {
    return !!value && typeof value === 'object' && !Array.isArray(value);
}
function hasOnlyKeys(value, allowed) {
    return isRecord(value) && Object.keys(value).every(key => allowed.includes(key));
}
function hasExactKeys(value, expected) {
    return hasOnlyKeys(value, expected) && expected.every(key => Object.prototype.hasOwnProperty.call(value, key));
}
function isAllowedExitCode(value) {
    return Object.values(EXIT_CODES).includes(value);
}
function normalizeExitCode(value, fallback) {
    return value !== undefined && isAllowedExitCode(value) ? value : fallback;
}
function protocolError(message) {
    return new HostedClientError('HOSTED_PROTOCOL', message);
}
function hostedFailure(body, fallback) {
    return new HostedClientError(body.error.code, body.error.message, body.error.help, normalizeExitCode(body.error.exitCode, fallback), {
        ...(body.execution ? { execution: body.execution } : {}),
        ...(body.trace ? { trace: body.trace } : {}),
        ...(body.error.details ? { details: body.error.details } : {}),
    });
}
function normalizeTraceMode(value) {
    return value === 'on' || value === 'retain-on-failure' ? value : 'off';
}
function isValidExecutedFailure(value, expectation) {
    if (!value.execution || !expectation || value.error.exitCode === undefined)
        return false;
    if (value.execution.command !== expectation.command)
        return false;
    const traceRequired = expectation.traceMode === 'on' || expectation.traceMode === 'retain-on-failure';
    return traceRequired ? value.trace !== undefined : value.trace === undefined;
}
