export type ParsedHostedCoreCommand = {
    command: 'validate';
    target?: string;
    format: string;
    formatExplicit: boolean;
} | {
    command: 'verify';
    target?: string;
    smoke: boolean;
    format: string;
    formatExplicit: boolean;
} | {
    command: 'convention-audit';
    target?: string;
    site?: string;
    strict: boolean;
    format: string;
    formatExplicit: boolean;
} | {
    command: 'doctor';
    verbose: boolean;
    format: string;
    formatExplicit: boolean;
};
/** Normalize a hosted built-in's format, preserving Commander-style usage errors. */
export declare function validateHostedFormat(raw: string): string;
export declare function parseHostedCoreCommand(argv: readonly string[], literal: boolean): ParsedHostedCoreCommand;
