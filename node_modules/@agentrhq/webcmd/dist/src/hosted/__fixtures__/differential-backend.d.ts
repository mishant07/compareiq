export interface DifferentialBackend {
    url: string;
    requests: {
        method: string;
        path: string;
        body: string;
    }[];
    close(): Promise<void>;
}
/** Deterministic endpoints make byte-for-byte entrypoint comparisons meaningful. */
export declare function startDifferentialBackend(): Promise<DifferentialBackend>;
