export declare function normalizeOfficialPluginSource(source: string): string;
export declare function normalizeOfficialCatalogSource<T extends {
    id: string;
    source: string;
    manifestUrl: string;
}>(entry: T): T;
