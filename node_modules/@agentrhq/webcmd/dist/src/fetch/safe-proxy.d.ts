import { lookup as dnsLookup } from 'node:dns';
export interface SafeProxy {
    url: string;
    close(): Promise<void>;
    policyError(): Error | undefined;
}
export interface SafeProxyOptions {
    allowPrivate?: boolean;
    lookup?: typeof dnsLookup;
}
export declare function normalizeFetchHostname(hostname: string): string;
export declare function isSafeFetchHostname(hostname: string): boolean;
export declare function isSafeAddress(address: string): boolean;
export declare function createSafeProxy(options?: SafeProxyOptions): Promise<SafeProxy>;
