import type { ProductIdentity, ProductManifest, ProductResolution } from './model.js';
export declare function canonicalProductKey(urlOrHost: string): ProductIdentity;
export declare function parseProductManifest(raw: string | null): ProductManifest | undefined;
export declare function resolveProduct(url: string, manifests: ProductManifest[]): ProductResolution;
