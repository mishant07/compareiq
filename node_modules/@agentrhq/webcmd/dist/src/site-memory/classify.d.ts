import { type LocalStoreOptions } from './local-store.js';
import type { ClassifyDecision, ClassifyResult, MemoryRevision } from './model.js';
export interface ClassifyInput extends LocalStoreOptions {
    requested: string;
    decision: ClassifyDecision;
    parent?: string;
    expectedRevision: MemoryRevision | null;
}
export declare function classifyProduct(input: ClassifyInput): Promise<ClassifyResult>;
