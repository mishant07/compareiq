import { type LocalStoreOptions } from './local-store.js';
import type { CandidateDisposition, CheckpointReason, CheckpointResult, MemoryRevision } from './model.js';
export interface CheckpointInput extends LocalStoreOptions {
    product: string;
    taskId: string;
    expectedRevision: MemoryRevision | null;
    reason: CheckpointReason;
    paths: string[];
    dispositions?: CandidateDisposition[];
}
export declare function checkpointMemory(input: CheckpointInput): Promise<CheckpointResult>;
