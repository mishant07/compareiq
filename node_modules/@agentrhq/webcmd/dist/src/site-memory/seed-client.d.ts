import type { SeedLookupResult } from './model.js';
export interface GlobalSeedProvider {
    lookup(productKey: string, signal?: AbortSignal): Promise<SeedLookupResult>;
}
export declare const DEFAULT_GLOBAL_MEMORY_URL = "https://api.webcmd.dev";
export declare function createHttpSeedProvider(options?: {
    fetch?: typeof fetch;
    env?: NodeJS.ProcessEnv;
}): GlobalSeedProvider;
