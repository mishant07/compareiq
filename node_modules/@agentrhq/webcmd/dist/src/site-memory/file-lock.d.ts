/** A lock held longer than this is treated as abandoned by a crashed process. */
export declare const LOCK_STALE_MS = 10000;
/** Total acquire budget. Longer than LOCK_STALE_MS so stale locks are broken, not reported. */
export declare const LOCK_TIMEOUT_MS = 15000;
/** Repository lock stale bound: copy, explicit staging, and two local commits, including slow Git. */
export declare const REPOSITORY_LOCK_STALE_MS = 60000;
/** Total repository-lock acquire budget. Longer than REPOSITORY_LOCK_STALE_MS so stale owners recover. */
export declare const REPOSITORY_LOCK_TIMEOUT_MS = 90000;
export interface FileLockOptions {
    staleMs?: number;
    timeoutMs?: number;
}
/** Lock marker for `target`, hidden from site memory readers by local-store. */
export declare function lockPathFor(target: string): string;
/** Run `fn` while holding the cross-process lock for `target`. */
export declare function withFileLock<T>(target: string, fn: () => Promise<T>, options?: FileLockOptions): Promise<T>;
