import { isIP } from 'node:net';
import * as os from 'node:os';
import { PKG_VERSION } from '../version.js';
const PUBLIC_IP_TIMEOUT_MS = 2000;
const PUBLIC_IP_URL = 'https://api.ipify.org';
export async function collectEnvironment(options = {}) {
    const env = {};
    if (options.machine !== false) {
        try {
            env.machine = (options.hostname ?? os.hostname)();
        }
        catch { }
    }
    if (options.localIp !== false) {
        try {
            const localIp = firstLocalIp();
            if (localIp)
                env.localIp = localIp;
        }
        catch { }
    }
    if (options.os !== false) {
        try {
            env.os = `${os.type()} ${os.release()}`;
        }
        catch { }
    }
    if (typeof options.browserVersion === 'string')
        env.browserVersion = options.browserVersion;
    if (options.webcmdVersion !== false) {
        env.webcmdVersion = typeof options.webcmdVersion === 'string' ? options.webcmdVersion : PKG_VERSION;
    }
    const envVars = options.env ?? process.env;
    if (options.publicIp !== false && envVars.WEBCMD_CANDIDATE_PUBLIC_IP !== 'off') {
        const publicIp = await lookupPublicIp(options.fetch ?? fetch);
        if (publicIp)
            env.publicIp = publicIp;
    }
    return env;
}
function firstLocalIp() {
    for (const entries of Object.values(os.networkInterfaces())) {
        for (const entry of entries ?? []) {
            if (!entry.internal && entry.family === 'IPv4')
                return entry.address;
        }
    }
}
async function lookupPublicIp(fetchFn) {
    try {
        const response = await fetchFn(PUBLIC_IP_URL, {
            signal: AbortSignal.timeout(PUBLIC_IP_TIMEOUT_MS),
            credentials: 'omit',
        });
        if (!response.ok)
            return;
        const value = (await response.text()).trim();
        return isIP(value) ? value : undefined;
    }
    catch {
        return undefined;
    }
}
