import { type SitesRepository } from './git-store.js';
import { type LocalStoreOptions } from './local-store.js';
import { type Candidate, type CandidateSummary } from './model.js';
export declare const SEARCH_CANDIDATE_LIMIT = 20;
export interface AddCandidateInput extends LocalStoreOptions {
    product: string;
    hostname?: string;
    kind: string;
    claim: string;
    evidence: string;
    consequence: string;
    observedAt?: string;
    environment?: Candidate['environment'];
    browserVersion?: string;
    webcmdVersion?: string;
    fetch?: typeof fetch;
}
export declare function addCandidate(input: AddCandidateInput): Promise<CandidateSummary>;
export declare function searchCandidates(product: string, query: string, limit?: number, opts?: LocalStoreOptions): Promise<CandidateSummary[]>;
export declare function listCandidates(product: string, opts?: LocalStoreOptions): Promise<CandidateSummary[]>;
export declare function showCandidate(product: string, id: string, opts?: LocalStoreOptions): Promise<Candidate>;
export declare function readCandidateRecord(product: string, id: string, opts?: LocalStoreOptions): Promise<Candidate>;
export declare function updateCandidateRecord(product: string, candidate: Candidate, opts?: LocalStoreOptions): Promise<void>;
export declare function recoverInterruptedProvenance(product: string, repo: SitesRepository, opts?: LocalStoreOptions): Promise<void>;
